{
  "name": "typeset",
  "version": "0.0.1",
  "scripts": {
    "start": "node ./typeset/start.js @*",
    "start:debug": "BROWSER_DEBUGGER=true node --inspect-brk ./typeset/start.js @*",
    "test": "standard --fix --verbose && jest",
    "watch": "jest --watch",
    "jest": "jest"
  },
  "dependencies": {
    "bunyan": ">=1",
    "bunyan-format": ">=0.2",
    "dotenv": ">=6",
    "file-exists": ">=5",
    "mathjax": "^2.7.4",
    "mathjax-node": ">=2",
    "pify": ">=3",
    "puppeteer": ">=1",
    "yargs": ">=15"
  },
  "devDependencies": {
    "@types/jest": ">=23",
    "jest": ">=26",
    "standard": ">=11"
  },
  "jest": {
    "coverageDirectory": "./coverage/",
    "collectCoverage": true
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
